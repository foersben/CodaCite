# Force uv to use a local cache within the project to avoid read-only system paths
cache-dir = ".cache/uv"

[pip]
# Ensures we always use the virtualenv in the project root
break-system-packages = true
